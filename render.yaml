services:
  - type: web
    name: bookmarket
    runtime: python
    region: frankfurt
    plan: free
    rootDir: bookmarket
    buildCommand: ./build.sh
    startCommand: gunicorn bookmarket.wsgi --bind 0.0.0.0:$PORT --workers 2 --threads 4 --worker-class gthread --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.4
      - key: DJANGO_DEBUG
        value: "False"
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DJANGO_SUPERUSER_USERNAME
        value: admin
      - key: DJANGO_SUPERUSER_EMAIL
        value: admin@bookmarket.tr
      - key: DJANGO_SUPERUSER_PASSWORD
        value: "BookMarket2026!"
      - key: DATABASE_URL
        fromDatabase:
          name: bookmarket-db
          property: connectionString

databases:
  - name: bookmarket-db
    region: frankfurt
    plan: free
